# Soft IP 主诉评估系统 — 产品开发计划

> 本文档是后续开发的**权威依据（Single Source of Truth）**。
> 维护原则：每一步设计**先与用户对齐、获明确确认后**才落到本文档；本文档更新后再动手写代码。
> 最后更新：2026-08-23

---

## 0. 协作铁律（最高优先级，永远遵循）

1. **诚实、严谨**：不编造、不糊弄，法条 / 数据 / 结论必须有据可依、可溯源。
2. **每一步设计先对齐再动手**：未经用户明确确认，不跳步、不擅自扩大改动范围。
3. **取证清单是前置依赖**：后续任何操作若必须依赖「商标侵权标准取证清单」才能开展，必须先做清单。

---

## 1. 项目现状

- 分支：`refactor/v13`（HEAD = 92db589），与 `origin/refactor/v13` 同步。
- 已完成：V13 架构重构（app.py 拆分 ui/services/llm/moot_court/state + 145 单元测试 + 外部 API 适配层）。
- 现有评分模型（**旧，待重构**）：`总分 = 法律可行性 × 业务预期 × 证据就绪度`。
- 技术栈：Streamlit + SQLAlchemy/SQLite + DeepSeek + 北大法宝 MCP + 企查查 MCP。
- 关键约束（**血的教训**）：用户机 Python 3.9。
  - 禁 PEP 604 类型语法（`X | Y`），改用 `Optional[X]` / `Union[X, Y]`。
  - 禁 f-string 表达式内反斜杠，改用 `.format()` 或变量提前赋值。
  - 禁 `match/case`（Python 3.10+）。
  - 每次改代码后必须用 Python 3.9 跑测试：
    ```
    /usr/local/Cellar/python@3.9/3.9.6/bin/python3.9 -m pytest tests/ -q
    /usr/local/Cellar/python@3.9/3.9.6/bin/python3.9 verify_product.py
    ```

---

## 2. 工作流总览

```
前置依赖（第 0 步）
   └─ 任务 #1：商标侵权标准取证清单（evidence_checklist.py）

工作流 A：三维评分模型重构（方案已对齐，待实施）
工作流 B：RAG 个性化知识库（分阶段，技术选型已对齐）

依赖关系：
   取证清单 ──► 工作流 A（证据前置化依赖它）
   取证清单 ──► 工作流 B 第 2 步（证据文档向量化落在同一界面，需界面结构稳定）
```

---

## 3. 前置依赖：商标侵权标准取证清单（任务 #1）

- **目标**：把埋藏在 `llm_client.py` / `mock_llm.py` 的 `evaluate_evidence_readiness` 里的取证清单，抽出来做成**带法律出处的标准取证清单**（结构化数据），作为 LLM 盘点证据的 prompt 输入。
- **来源（三层）**：
  1. 权威法条（需北大法宝核验）：法释〔2020〕12号《最高人民法院关于知识产权民事诉讼证据的若干规定》 + 商标法第 57/63/64 条。
  2. 项目已有骨架（权利基础证据 / 侵权认定证据 / 损害赔偿证据 / 取证技术规范 四类）。
  3. 证据缺口判断规则（复用 `legal_rules.py` 的红线逻辑）。
- **分工**：AI 抽骨架 + 北大法宝检索权威依据 + 用户实务核验 + 固化成清单数据（供 LLM 盘点使用）。

**进度**：
- ✅ 商标权清单已定稿并固化（`TRADEMARK_EVIDENCE_CHECKLIST`，11 项）。
- ✅ 著作权清单已定稿并固化（`COPYRIGHT_EVIDENCE_CHECKLIST`，10 项）。
- ✅ 不正当竞争清单已定稿并固化（`UNFAIR_COMPETITION_EVIDENCE_CHECKLIST`，9 项，基于反不正当竞争法 2025 修订版）。
- 测试：`tests/test_evidence_checklist.py` 共 22 项，Python 3.9 全绿（167 passed）。
- **三案由取证清单全部完成，任务 #1 收尾。**

---

## 4. 工作流 A：三维评分模型重构（方案已对齐）

**核心主张**：相乘的只能有两个 ——「能不能赢」×「值不值」。

```
评估层：
  ① 证据盘点（前置，LLM 调用，不占评分位）→ 产出「证据缺口清单」喂给②③
  ② 法律可行性 = 权利基础 × 侵权认定 × 诉讼程序 × 对抗修正系数（内部乘法保留）
  ③ 业务预期   = 判赔规模 × 回款能力
决策层：
  主诉决策分 = 法律可行性 × 业务预期
  证据完整度 → 不乘，作为「置信度」+「门禁」独立输出
  红线规则   → 硬门禁（block 直接不建议起诉，不看分数）
```

**四条已对齐的改动**：

1. 证据就绪度：从「第三平级乘法维度」降级为「前置输入 + 置信度」。
2. 法律可行性 × 业务预期是唯一正确的相乘对，内部乘法保留。
3. 业务预期内部拆出「回款能力」显性化（企查查被告画像产出回款概率），业务预期 = 判赔规模 × 回款能力。
4. 红线规则升格为硬门禁。

**两个已确认的判断**：

- 时间不单列维度，作为业务预期的子因子（折现进净现值）。
- 证据盘点用 LLM 调用（带标准取证清单作为 prompt 输入）。

---

## 5. 工作流 B：RAG 个性化知识库（分阶段）

**目的**：用户对评估结果不认同 / 有想法时，能在两个位置与评估模型交互。

### 5.1 技术选型（已对齐）

| 项 | 选择 |
|----|------|
| 向量数据库 | ChromaDB（轻量、本地持久化、Streamlit 单机事实标准） |
| Embedding | 硅基流动 bge-m3（API，OpenAI 兼容；DeepSeek 无 embedding 接口，需新接入） |
| 知识库粒度 | 全局个人知识库（跨案件共享） |
| 追问形态 | 多轮对话 |
| 追问回写 | 生成第二版报告（独立于原报告，版本化并存） |

### 5.2 两个交互位置

| 位置 | 交互 | 作用 |
|------|------|------|
| ① 评估前（新建案件 / 上传证据界面） | 一键接入知识库 **或** 文字输入观点 | 用户观点作为后续评估的重要提示词 |
| ② 评估后（结果界面） | 接入知识库 **或** 文字追问 | 追问某条分析、表达疑问，AI 详细解答，改进后生成第二版报告 |

### 5.3 知识库来源（ABCD 全部要，分阶段落地）

四者共享同一套 RAG 底座（ChromaDB + bge-m3 + 写入/检索/删除 API），只是入口不同：

| 来源 | 说明 | 难度 | 阶段 |
|------|------|:---:|:---:|
| A 证据文档向量化 | 文档 → 切块(chunking) → 向量化入库；复用现有 `evidence_parser.py` | 中 | 第 2 步 |
| B 手动粘贴文本 | 文本框 + 入库按钮 | 低 | 第 1 步 |
| C 观点自动沉淀 | 用户输入观点/追问时顺手向量化入库 | 低 | 第 1 步 |
| D 独立建库入口 | 管理页（增删改查 + 上传文档），复用 A 的文档入库 | 中低 | 第 3 步 |

### 5.4 分阶段规划

```
第 0 步：取证清单（任务 #1，前置依赖）——先定证据上传界面结构
第 1 步：RAG 底座 + C + B      ——短文本入口，几乎零额外成本，先跑通
第 2 步：A 证据文档向量化       ——含 chunking，唯一技术难点
第 3 步：D 独立建库入口        ——复用 A 的文档入库，加管理 UI
```

### 5.5 已确认细节

- 「一键接入」= 用户自己在知识库中选择要接入的内容（系统不自动检索带入），选择权在用户，可搜索辅助。
- 观点注入 = 全部 7 步评估 prompt 都带上用户观点上下文。
- 追问回写 = AI 改进后生成第二版报告，独立于原报告（版本化并存，不覆盖）。

---

## 6. 执行顺序（总排期）

| 阶段 | 内容 | 依赖 | 状态 |
|------|------|------|------|
| 第 0 步 | 取证清单（`evidence_checklist.py`） | 无 | ⬜ 待办 |
| 第 1 步 | RAG 底座 + C 观点沉淀 + B 手动粘贴 | 第 0 步 | ⬜ 待办 |
| 第 2 步 | A 证据文档向量化（chunking） | 第 0 步 + 第 1 步 | ⬜ 待办 |
| 第 3 步 | D 独立建库入口 | 第 2 步 | ⬜ 待办 |
| 工作流 A | 三维评分模型重构 | 第 0 步（证据前置化依赖取证清单） | ⬜ 待办 |

---

## 7. 关键约束与测试要求

- 所有新增模块必须有对应单元测试。
- 每次重构后必须跑完整烟雾测试（10 项）验证产品手册核心链路未破坏。
- Python 3.9 兼容性规则（见第 1 节）贯穿始终。
- 法条、司法解释等法律内容一律经北大法宝核验后落地，禁止凭记忆书写。
